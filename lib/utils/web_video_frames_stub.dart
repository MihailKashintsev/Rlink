import 'dart:typed_data';

Future<List<Uint8List>> webVideoThumbnails(String url, {int count = 8}) async =>
    const [];

Future<Uint8List?> webVideoPoster(String url, {int maxSide = 480}) async => null;
